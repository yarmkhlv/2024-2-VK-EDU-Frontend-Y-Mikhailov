import { PAGES } from '../../../../../utils/variables';
import { INavItem } from './types';

export const NAV_DATA_ITEMS: INavItem[] = [
    { text: 'Главная', link: PAGES.TRANSLATOR },
    { text: 'История', link: PAGES.HISTORY },
];
